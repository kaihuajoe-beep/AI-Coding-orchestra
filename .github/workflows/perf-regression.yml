name: Perf Regression

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Simple perf check
        run: |
          echo "Performance benchmark placeholder"
          echo "Add project-specific benchmarks here"
          # 示例: python -m pytest --benchmark-only
      - name: Summary
        run: echo "✅ Perf regression check passed"
